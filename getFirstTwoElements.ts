// Função para obter os dois primeiros elementos de um array
function getFirstTwoElements(array: number[]): number[] {
    return array.slice(0, 2);
}

// Exportando a função para facilitar os testes
export { getFirstTwoElements };
